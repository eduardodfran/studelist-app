{
  "version": 2,
  "builds": [
    {
      "src": "./server/server.js",
      "use": "@now/node",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "./server/server.js"
    }
  ]
}
